# 尚未实测。ATM10 7.1 的内置资源包顺序必须**真起一次实例、干净退出**才拿得到——
# options.txt 是退出时才写完整列表的，中途 kill 掉只会得到 resourcePacks:[]。
#
# 已知的部分事实（7.1 实例真启动过，从资源包加载顺序读出来的）：
#   7.2 那 15 项里有 14 项在本版也存在，唯独缺 `modern_industrialization/generated`
#   ——本版的 Modern Industrialization 是 2.4.3，那个内置包是 2.5 才加的。
#   这恰好证明各版列表确实不同，不能拿 7.2 的凑合。
#
# 但「14 项」是**推导**不是直读，万一还漏了别的，汉化包就会被压在下面而毫无提示。
# 所以这里保持为空：安装器在全新实例上**不伪造** resourcePacks 行，
# 只写 lang:zh_cn，并提示玩家先启动一次游戏、退出后再运行一次安装器——
# 那一次会把汉化包挪到列表最后一位，必定正确。
